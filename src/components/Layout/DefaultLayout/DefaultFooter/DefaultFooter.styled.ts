import styled from 'styled-components';

export const DefaultFooterContainer = styled.header`
    margin-top: auto;
    padding: 4rem 2rem;
    background-color: ${({ theme }) => theme.color.tertiary.background};
`;
